{
  "name": "grail-chapter1-html5-prototype",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.mjs",
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test tests/ && node tests/smoke.mjs"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
